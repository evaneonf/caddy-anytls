services:
  caddy-anytls:
    build:
      context: .
      dockerfile: Dockerfile
    image: caddy-anytls:local
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./config:/etc/caddy:ro
      - caddy_data:/data
    restart: unless-stopped

volumes:
  caddy_data:
